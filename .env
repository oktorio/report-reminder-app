# === SMTP settings ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=suptech.rnd@gmail.com
SMTP_PASSWORD=Ojkisaca1!
SMTP_USE_TLS=true
SENDER_NAME=Nama SupTech
SENDER_EMAIL=suptech.rnd@gmail.com
EMAIL_DRY_RUN=false


# If true, emails won't be sent; they will be logged to console only.
EMAIL_DRY_RUN=true

# === App settings ===
FLASK_SECRET_KEY=change-this-secret
DATABASE_URL=sqlite:///data.db
APP_TZ=Asia/Jakarta

# Default reminder offsets in days (comma-separated). Used when schedule has no custom offsets.
DEFAULT_REMINDER_OFFSETS=7,3,1,0

# When the daily reminder job runs (24h format, local TZ)
DAILY_JOB_HOUR=8
DAILY_JOB_MINUTE=0
